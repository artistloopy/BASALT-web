---
import { Icon } from 'astro-icon/components';

export interface Props {
  icon?: string;
  title?: string;
  description?: string;
}

const {
  icon = 'tabler:info-square',
  title = await Astro.slots.render('title'),
  description = await Astro.slots.render('description'),
} = Astro.props;
---

<section class="bg-[#006994] dark:bg-[#004f66] not-prose">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-4 text-md text-center font-medium text-white">
    {icon ? <Icon name={icon} class="w-5 h-5 inline-block align-text-bottom font-bold text-white" /> : null}
    <span class="font-bold" set:html={title} />
    <Fragment set:html={description} />
  </div>
</section>
